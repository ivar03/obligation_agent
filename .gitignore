# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Node / Next.js
node_modules/
.next/

# Secrets — ignore every .env variant, but keep the committed templates
.env
.env.*
!.env.example
!.env.production.example

# Local SQLite databases (dev/test artifacts, regenerated by seed.py)
*.db

# Agent workspace (local planning/ledger scratch)
.superpowers/

# NOTE: `docs` was ignored here previously. docs/operations/ and docs/security/
# are already tracked, so the rule only blocked NEW docs from being added.
# Re-add a narrower rule if you meant to exclude a specific folder.
docs